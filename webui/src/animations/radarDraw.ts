// Animation #5 — radar draw-in (Chart.js built-in animation)
export const radarDrawOptions = {
  animation: {
    duration: 1200,
    easing: "easeOutQuart" as const,
  },
  responsive: true,
  maintainAspectRatio: false,
};

export const radarDrawInMs = 1200;
