# Morning job-list keyboard navigation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `j`/`k` and arrow keys move `aria-current` between morning job cards via `showJob`, without stealing typing or inventing `FOUND`.

**Architecture:** Extend the window `keydown` listener. A `moveJobSelection(delta)` helper reads `#job-list button` nodes. Hint copy is locked.

**Tech Stack:** Packaged static JS, pytest, Playwright e2e.

**Spec:** `docs/requirements/2026-08-20-job-keys.md`

## Global Constraints

- Do not invent `FOUND`. Do not change Help / `HOST_CONSTRAINT` / EXAMPLE_SPEC.
- Do not change Ctrl/Cmd+Enter. No FakeWorker in morning e2e. Do not unfreeze `webui/`.
- Do not shrink DSR `N`. Do not start soak jobs.

---

### Task 1: Keys + hint

**Files:**
- Modify: `src/alphaloop/webui/static/index.html`
- Modify: `src/alphaloop/webui/static/app.js`
- Modify: `src/alphaloop/webui/static/styles.css`
- Modify: `docs/webui.md`, `mkdocs.yml`
- Test: `tests/runtime/test_static_console.py`
- Test: `tests/e2e/test_morning_console.py`

- [x] **Step 1: Failing tests**

```python
def test_packaged_console_job_list_keys():
    html = ...
    assert 'id="job-keys-hint"' in html
    assert "j/k or arrows move between jobs." in html
    assert "ArrowDown" in script
    assert "ArrowUp" in script
    assert "showJob(" in script
    assert "TEXTAREA" in script
    assert "INPUT" in script
    assert "#job-keys-hint" in css
    assert "http" not in css
```

E2E home: hint text.

E2E `test_arrow_down_moves_aria_current_to_older_job`: freeze twice (same YAML, Freeze stays enabled after first submit), wait for two `#job-list button`, first has `aria-current=true`, `page.keyboard.press("ArrowDown")`, second has `aria-current=true`.

- [x] **Step 2: FAIL**

```bash
python3 -m pytest tests/runtime/test_static_console.py::test_packaged_console_job_list_keys -v
```

- [x] **Step 3: Implement**

`moveJobSelection(delta)`, extend keydown, hint, CSS, docs, mkdocs.

- [x] **Step 4: Tests pass**

- [x] **Step 5: Commit**

```bash
git commit -m "feat(webui): j/k and arrows move between morning jobs"
```
